# Nuxt dev/build outputs
.nuxtrc
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Test coverage output
coverage

# Playwright e2e output
test-results
playwright-report
blob-report

# Logs
logs
*.log

# ONE-Mobile-POP webfont, fetched by scripts/fetch-fonts.mjs at install time.
# Deliberately NOT committed: its license permits embedding on our own server
# but prohibits redistribution, and this is a public MIT repo -- committing
# the file would make it trivially extractable as a standalone asset. See
# scripts/fetch-fonts.mjs's header comment for the full reasoning.
public/fonts/

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
